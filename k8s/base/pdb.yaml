# During voluntary disruptions (node drain, cluster upgrade) keep at least
# one backend pod serving.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: backend-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: backend
